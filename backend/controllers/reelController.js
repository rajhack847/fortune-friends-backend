// Reels controller removed. Placeholder to avoid import errors.
export const uploadReel = async (req, res) => {
	res.status(410).json({ success: false, message: 'Reels removed' });
};

export const getReels = async (req, res) => {
	res.status(410).json({ success: false, message: 'Reels removed' });
};

export const deleteReel = async (req, res) => {
	res.status(410).json({ success: false, message: 'Reels removed' });
};

export const addReelComment = async (req, res) => {
	res.status(410).json({ success: false, message: 'Reels removed' });
};

export const getReelComments = async (req, res) => {
	res.status(410).json({ success: false, message: 'Reels removed' });
};

export const subscribeReelUser = async (req, res) => {
	res.status(410).json({ success: false, message: 'Reels removed' });
};

export const getReelSubscribers = async (req, res) => {
	res.status(410).json({ success: false, message: 'Reels removed' });
};
